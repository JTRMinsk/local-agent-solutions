# llama.cpp GPU Memory Optimization (auto-fit)

## 📝 问题场景
在加载 GGUF 模型时，如果手动设置 `-ngl` (number of GPU layers) 参数，往往难以达到显存利用率的最优状态：
- **设置过高**：导致显存溢出 (OOM)，程序崩溃。
- **设置过低**：大量模型层保留在 CPU/内存中，导致推理速度无法达到 GPU 的峰值性能。

## ✅ 解决方案

### 1. 指令优化 (Instruction Optimization)
**不要手动设置 `-ngl` 参数**。

### 2. 原理说明 (Principle)
通过不传 `-ngl` 参数，让 `llama-server` 在启动阶段执行自动探测：
- **自动探测**：结合当前系统的剩余显存信息与模型的架构参数。
- **动态计算**：自动计算出能够完全装入 GPU 显存的最大层数，实现“刚好填满”的效果。

## 🛠️ 验证案例
通过对 `Q8_0 12GB` 模型的测试验证：
- 使用 `auto-fit` 模式下，显存占用比手动猜测更稳健。
- 运行效率最高，且有效避免了因手动设置不当导致的 OOM。
